model DocumentType {
  id        String     @id @default(uuid()) @db.Uuid
  name      String     @unique
  code      String     @unique
  isActive  Boolean    @default(true)
  suppliers Supplier[]

  @@index([code])
  @@map("document_types")
}