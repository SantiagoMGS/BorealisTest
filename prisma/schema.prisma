generator client {
  provider      = "prisma-client-js"
  binaryTargets = ["native"]
  output          = "./node_modules/@prisma/client"

}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
