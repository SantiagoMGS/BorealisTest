generator client {
  provider      = "prisma-client-js"
  binaryTargets = ["native"]
 // output        = "./generated/client"

}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
